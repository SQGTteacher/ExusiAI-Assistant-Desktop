# 课堂点名器

面向课堂大屏的本地点名插件。名单和课堂记录只保存在本机，不需要网络权限。

## 首版功能

- 导入 CSV、TSV 或每行一个姓名的 TXT 名单
- 自动识别“姓名 / name”“学号 / id”“班级 / class”等表头
- 每轮不重复的随机点名，可标记缺席并撤销最近一次点名
- 自动保存当前名单和本轮进度
- 将课堂记录导出为 UTF-8 CSV

后续版本计划增加 XLSX 导入、多班级切换和名单图片 OCR；这些能力不会阻塞当前的轻量使用。
