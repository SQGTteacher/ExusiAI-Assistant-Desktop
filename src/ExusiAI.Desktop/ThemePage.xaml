<UserControl x:Class="ExusiAI.Desktop.ThemePage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="主题设置" Style="{StaticResource PageTitle}" />
        <TextBlock Text="选择最适合当前教室光线的显示方式。" Style="{StaticResource PageSubtitle}" />
        <ListBox ItemsSource="{Binding Options}" SelectedItem="{Binding SelectedOption}" BorderThickness="0" Background="Transparent" Margin="0,24,0,0">
            <ListBox.ItemsPanel><ItemsPanelTemplate><UniformGrid Columns="3" /></ItemsPanelTemplate></ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="Margin" Value="5" /><Setter Property="Padding" Value="0" /><Setter Property="HorizontalContentAlignment" Value="Stretch" /><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><Border x:Name="Card" Style="{StaticResource Card}" Padding="20"><ContentPresenter /></Border><ControlTemplate.Triggers><Trigger Property="IsSelected" Value="True"><Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /><Setter TargetName="Card" Property="BorderThickness" Value="2" /></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style></ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate><DataTemplate><StackPanel><Border Height="74" CornerRadius="10" Background="{DynamicResource SurfaceAltBrush}" Margin="0,0,0,14"><Grid><Border Width="72" Height="8" CornerRadius="4" Background="{DynamicResource AccentBrush}" /><Border Width="34" Height="34" CornerRadius="10" Background="{DynamicResource SurfaceBrush}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" /></Grid></Border><TextBlock Text="{Binding Name}" FontSize="17" FontWeight="SemiBold" /><TextBlock Text="{Binding Description}" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,6,0,0" /></StackPanel></DataTemplate></ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</UserControl>
